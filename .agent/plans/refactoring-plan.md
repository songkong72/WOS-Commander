# 📋 프로젝트 리팩토링 계획서

> **최종 업데이트**: 2026-02-21
> **원칙**: 세션별 1모듈 분리 → 빌드 확인 → 커밋 → 다음 세션

---

## ✅ 완료된 작업

### ~~events.tsx 인라인 모달 → 컴포넌트 교체~~

| # | 항목 | 줄 수 | 상태 |
|---|---|---|---|
| ~~1-1~~ | ~~**Attendee Modal** → `AttendanceModal`~~ | ~~198줄~~ | ✅ 완료 |
| ~~1-2~~ | ~~**Date Picker Modal** → `DatePickerModal`~~ | ~~107줄~~ | ✅ 완료 |
| ~~1-3~~ | ~~**Custom Alert Modal** → `CustomAlert`~~ | ~~53줄~~ | ✅ 완료 |
| ~~1-4~~ | ~~**Warning Modal** → `WarningModal`~~ | ~~41줄~~ | ✅ 완료 |
| ~~1-5~~ | ~~**미사용 import/state 정리** (BlurView, FlatList 등)~~ | ~~30줄~~ | ✅ 완료 |

📊 **events.tsx: 3,170줄 → 1,611줄** (1,559줄 감소, 49%)


### ~~index.tsx 세션 1: 유틸 함수 분리~~

| 항목 | 상태 |
|---|---|
| ~~`app/utils/eventHelpers.ts` 생성~~ | ✅ 완료 (197줄) |
| ~~`app/utils/dynamicFontSize.ts` 생성~~ | ✅ 완료 (26줄) |

### ~~index.tsx 세션 2: 이벤트 상태 판정 로직 분리~~

| 항목 | 상태 |
|---|---|
| ~~`app/utils/eventStatus.ts` 생성~~ | ✅ 완료 (455줄) |

### ~~index.tsx 세션 3: Gate/로딩 화면 분리~~

| 항목 | 상태 |
|---|---|
| ~~`app/hooks/useGateLogic.ts` 생성~~ | ✅ 완료 (70줄) |

### ~~기타~~

| 항목 | 상태 |
|---|---|
| ~~shadow-* deprecated 경고 수정~~ | ✅ 완료 (global.css) |

---

### ~~index.tsx 세션 4: 이벤트 카드 + 대시보드 부품 분리~~

| 항목 | 상태 |
|---|---|
| ~~`components/events/EventCard.tsx` 생성 및 연결~~ | ✅ 완료 |
| ~~`components/dashboard/EventSectionHeader.tsx` 생성 및 연결~~ | ✅ 완료 |
| ~~`components/dashboard/DashboardCards.tsx` 생성 및 연결~~ | ✅ 완료 |
| ~~`components/dashboard/EventSectionList.tsx` 생성 및 연결~~ | ✅ 완료 |

📊 **index.tsx: 1,170줄 → 713줄** (457줄 감소, 39%)

### ~~기타~~

| 항목 | 상태 |
|---|---|
| ~~shadow-* deprecated 경고 수정~~ | ✅ 완료 (global.css) |

---

## ⬜ 남은 작업

### 세션 5: 중복/미사용 코드 정리 및 공통화

| # | 항목 | 설명 | 상태 |
|---|---|---|---|
| 2-1 | 구 `EventCard` 완전 제거 | `GrowthEventCard`와 통합 또는 `EventCard` 단일화 검토 | ⬜ 대기 |
| 2-2 | `showCustomAlert` 공통화 | 11개 파일에서 개별 정의 → 전역 Context 또는 Hook으로 일원화 | ⬜ 대기 |
| 2-3 | `getKoreanDayOfWeek` 중복 제거 | `app/utils/eventHelpers.ts`에 있는 버전으로 통합 | ⬜ 대기 |
| 2-4 | `parseScheduleStr` 중복 제거 | 여러 곳에 흩어진 날짜 파싱 로직 통합 | ⬜ 대기 |

---

### 세션 6: 커스텀 Hook 추출 (고급)

| # | 대상 | 현재 위치 | 상태 |
|---|---|---|---|
| 3-2 | `useAttendeeManager` hook | events.tsx (벌크 참가자 관리 로직) | ⬜ 대기 |
| 3-3 | `useEventFilter` hook | events.tsx (카테고리, 검색, 필터 로직) | ⬜ 대기 |
| 3-4 | `useDashboard` hook 고도화 | index.tsx의 남은 state들을 dashboard hook으로 추가 이동 | ⬜ 대기 |

---

### 세션 7: 대형 컴포넌트 추가 분리

| # | 파일 | 현재 크기 | 목표 | 상태 |
|---|---|---|---|---|
| 4-1 | `components/TimelineView.tsx` | 967줄 | 타임라인 그리드 / 툴팁 분리 | ⬜ 대기 |
| 4-2 | `components/AdminManagement.tsx` | 826줄 | 설정/멤버/요청 탭별 분리 | ⬜ 대기 |
| 4-3 | `app/hooks/useAdminAuth.ts` | 785줄 | 권한 체크 로직 세분화 | ⬜ 대기 |

---

## 📁 현재 파일 크기 (2026-02-22 측정)

```
app/growth/events.tsx        1,671줄
components/TimelineView.tsx    967줄
app/screens/GateScreen.tsx     863줄
components/AdminManagement.tsx 826줄
app/hooks/useAdminAuth.ts      785줄
app/index.tsx                  713줄 (핵심 로직만 잔류)
components/events/EventCard.tsx 473줄
components/dashboard/EventSectionList.tsx 222줄
```

---

## 🛡️ 안전 규칙

1. **한 세션에 한 모듈만** 분리
2. **반드시 빌드 확인** 후 커밋
3. 분리 시 **원본 동작 변경 금지** (리팩토링만, 기능 변경 없음)
4. **import 경로** 꼼꼼히 확인 (상대 경로 주의)
5. 분리 대상 함수가 컴포넌트 내부 state에 의존하면 **파라미터화**
6. TypeScript 타입은 별도 `types.ts`로 분리 검토
